# DeepSeek API Configuration
# Get your API key from https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Optional: API endpoint (default is https://api.deepseek.com)
# DEEPSEEK_API_ENDPOINT=https://api.deepseek.com

# Optional: Model name (default is deepseek-chat)
# DEEPSEEK_MODEL=deepseek-chat

# Optional: Max parallel workers for translation (default is 25)
# MAX_WORKERS=25

# Optional: Tokens limit per request (default is 4000)
# MAX_TOKENS=4000

# Optional: Temperature for translation (0.0-1.0, default is 0.3)
# TEMPERATURE=0.3